<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/dogus_card_background_modern"
    android:layout_margin="4dp"
    android:gravity="center_vertical">

    <!-- Thumbnail Image -->
    <ImageView
        android:id="@+id/ivThumbnail"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/dogus_image_placeholder"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp"
        android:contentDescription="Reklam Önizleme"/>

    <!-- Advertisement Details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Reklam Başlığı"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/dogus_text_dark"
            android:layout_marginBottom="4dp"
            android:maxLines="1"
            android:ellipsize="end"/>

        <!-- Type and Duration -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="4dp">

            <TextView
                android:id="@+id/tvType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="📷 Fotoğraf"
                android:textSize="14sp"
                android:textColor="@color/dogus_primary"
                android:layout_marginEnd="16dp"/>

            <TextView
                android:id="@+id/tvDuration"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="5 saniye"
                android:textSize="14sp"
                android:textColor="@color/dogus_text_secondary"/>

        </LinearLayout>

        <!-- File Size -->
        <TextView
            android:id="@+id/tvFileSize"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2.5 MB"
            android:textSize="12sp"
            android:textColor="@color/dogus_text_secondary"/>

    </LinearLayout>

    <!-- Action Indicator -->
    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@drawable/ic_arrow_right"
        android:contentDescription="Detaylar"
        android:layout_marginStart="8dp"
        android:alpha="0.6"/>

</LinearLayout>
